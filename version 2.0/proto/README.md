# V2 Protocol Schema

This folder contains the first-pass schema definitions for V2.

Design rules:

- binary-first
- resumable event streams
- remote-first session model
- relay-backed default transport
- optional direct-path negotiation

The files here are schema drafts, not finalized compatibility contracts.
