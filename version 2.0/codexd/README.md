# codexd

Rust daemon for Codex Remote V2.

Current scope:

- config loading
- local trust store bootstrap
- placeholder runtime supervisor
- relay-backed remote presence loop
- local health endpoint

This is the first executable daemon skeleton, not the finished V2 runtime.
